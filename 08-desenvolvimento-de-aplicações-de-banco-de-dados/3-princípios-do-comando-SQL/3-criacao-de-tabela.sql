select * from scott.dept;
-- a tabela dept pertence ao schema scott
-- o nome da tabela é dept
-- o oracle live precisa identificar a tabela todo. O nome do usuário ponto e o nome da tabela

select deptno, dname from scott.dept;
-- especificando as colunas que queremos selecionar, ver.